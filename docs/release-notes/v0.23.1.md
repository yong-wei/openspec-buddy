# v0.23.1

## Fixed

- Made the review-wait cache-refresh eval retry once with twice the outer
  wall-clock budget when the first run is terminated by timeout.
- Isolated the cache-refresh scenario from inherited cache configuration and
  cleared its cache before both the initial run and retry, preventing stale but
  internally consistent cache files from hiding refresh regressions.
- Ensured timeout cleanup terminates descendant processes before resetting
  shared test state.

## Verification

- `npm test`
- `npm pack --dry-run`
- `git diff --check`
