# v0.21.0

## Added

- Added fail-closed validation for ambiguous status labels and conflicting
  coupling metadata/labels during issue selection and claim preflight.
- Added complete coupling-group evidence checks for metadata, labels, and
  bodyless active issues.

## Fixed

- Preserved the candidate-selection limit without allowing it to truncate the
  coupling mutual-exclusion gate.
- Made numeric REST issue limits stop after the required page instead of
  fetching every open issue; full pagination is now explicit through `all`.
- Added regression coverage for bounded pagination, status ambiguity, coupling
  conflicts, and the full claim/selector path.

## Verification

- `npm test`
- `npm run test:fast`
- `npm pack --dry-run`
- `git diff --check`
