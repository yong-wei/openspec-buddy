# v0.29.0

## Claim agent attribution

- Claim comment 的 `agent` 字段改为记录实际 `harness/model`，不再使用 GitHub 用户名生成代理身份。
- 新增 `OPENSPEC_BUDDY_AGENT` 运行时配置，例如 `codex/gpt-5.6-sol`、`zcode/glm-5.3` 或 `grok/grok-4.6`。
- GitHub comment author 与 assignee 成为认领者身份真源；更换模型不会被误判为 claim 所有权变化。
- 旧版 `agent: @username` 与 `agent: codex/username` comment 保持可读取兼容。
- Lite 与 Full Claim 在任何 GitHub 写入前校验运行时 agent identity。

## Verification

- Claim、selector、live claim truth、release claim 与配置回归测试
- `npm test`
- `npm pack --dry-run`
- `git diff --check`
