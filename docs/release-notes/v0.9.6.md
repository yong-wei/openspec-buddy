# v0.9.6

## Fixes

- Added `review-response-gate.sh` to require reply evidence before resolving actionable Codex `P0/P1/P2` review threads.
- Blocked `request-pr-review.sh`, `wait-for-review-clear.sh`, and `mark-achieved.sh` when unresolved actionable Codex review threads remain.
- Added `verify-review-threads-resolved.sh` as the final unresolved-thread guard.
- Made review thread gating fail closed when GraphQL pagination indicates truncated review threads or thread comments.
- Updated Buddy and Buddy Auto docs so review-fix commits are complete only after reply, resolve, and GraphQL verification.

## Validation

- `npm test`
- `npm pack --dry-run`
- subagent code review passed
