# v0.31.1

## CI 兼容性修复

- `evals/lite/worktree-base.test.mjs` 的 clone 夹具补充本地 git 身份配置；
  此前该测试依赖开发机的全局 git 身份，导致 v0.31.0 的 npm 发布工作流在
  Verify package 阶段失败。v0.31.0 因此未发布到 npm，由本版本取代。
- `wait-for-review-clear.test.sh` 放宽高负载下的时序余量：所有要求 helper
  在限定时间内给出结论的场景统一加宽外层超时，head-change 场景的
  `MAX_WAIT` 同步加宽；断言语义不变。

## Verification

- `npm test`
- `npm pack --dry-run`
- `git diff --check`
