# v0.9.5

## Fixes

- Changed issue-backed Buddy Auto selection to choose the oldest executable issue first, using the smallest issue number globally.
- Kept current series, downstream blocking count, and risk as diagnostics instead of priority keys for issue-backed selection.
- Preserved safe review-clear REST cache reuse: direct verification refreshes PR REST state by default, while wait-mode reuses cache only after its own light REST refresh.
- Updated review wait/request eval stubs for paginated REST bundle calls.

## Validation

- `npm test`
- `npm pack --dry-run`
- subagent code review passed
