# v0.17.0

## Fixed

- Restored Buddy Auto goal-loop continuation by allowing explicitly authorized
  goal mode to run the selector from an empty issue/PR context.
- Added `OPENSPEC_BUDDY_AUTO_GOAL=1` and `--goal` as the only empty-context path
  that may select the next change; issue-backed selections are claimed and
  local-only selections enter the existing local review handoff.
- Prevented goal mode from being overwritten by an ambient historical PR on the
  coordination branch.

## Verification

- `node skills/openspec-buddy-auto/evals/buddy-auto-driver.test.mjs`
- `npm test`
- `npm pack --dry-run`
