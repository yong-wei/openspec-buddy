# v0.9.7

## Fixes

- Added a hard worktree claim guard for Buddy execution-sensitive steps.
- Recorded worktree identity in claim comments and local cache for auditability.
- Blocked detached execution, foreign worktree branch ownership, PR head mismatches, and PR-only review/merge paths without an active origin issue claim.
- Reduced partial claim drift by writing the claim comment before setting `status:claimed`.

## Verification

- `npm test`
- `npm pack --dry-run`
- Subagent code review: passed with no blocking findings.
