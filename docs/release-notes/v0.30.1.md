# v0.30.1

## Release verification compatibility

- 将 Auto Lite 的 direct-claim 路径说明保留在 Manual Buddy 文档，避免扩展
  Auto Lite 公共 skill 的既有入口范围和长度契约。
- `v0.30.0` 的 direct-claim proposal adoption 实现保持不变；此补丁仅修复
  发布工作流的 skill-contract 验证失败。

## Verification

- `npm test`
- `npm pack --dry-run`
- `git diff --check`
